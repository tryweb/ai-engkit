# ai-engkit Environment Variables
# IMPORTANT: copy this file to .env before running docker compose.
#   cp .env.example .env
#
# Without .env, docker compose will fall back to the shell environment
# (often empty or stale), and your custom password edits will be silently
# ignored. .env is in .gitignore so your local values are not committed.
#
# Multi-container architecture:
# - AI engine (opencode serve) runs in the `ai-engine` container
# - Web UI (openchamber serve) runs in the `ai-ui` container
# - OPENCODE_* vars -> ai-engine; OPENCHAMBER_* vars -> ai-ui

# === OpenCode Settings ===
# OpenCode server password (required for API access)
OPENCODE_SERVER_PASSWORD=devonly

# OpenCode plugins to load (comma-separated).
# Set here in .env (not via shell export) to avoid host environment leaking into containers.
OPENCODE_PLUGINS=oh-my-openagent,superpowers@git+https://github.com/obra/superpowers.git

# === UI Settings ===
# Web UI password (required for browser authentication)
OPENCHAMBER_UI_PASSWORD=ai-engkit-dev

# === Ports ===
# Host port for OpenChamber Web UI
CHAMBER_PORT=8000

# === Workspace ===
# Workspace volume type (v0.5.0+ uses named volumes by default):
# - Leave commented/unset: Uses Docker named volume `workspace` (recommended, fully managed)
# - Set to host path: Uses bind mount (e.g., ./workspace, /home/user/projects)
#   Useful for direct file editing with local IDE
# WORKSPACE_PATH=./workspace
